apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-service.fullname" . }}
  labels: {{- include "agent-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector: {{- include "agent-service.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "agent-service.fullname" . }}
  labels: {{- include "agent-service.labels" . | nindent 4 }}
