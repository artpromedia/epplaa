apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "api-monolith.fullname" . }}
  labels: {{- include "api-monolith.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
