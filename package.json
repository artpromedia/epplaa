{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "prepare": "tsc --build || true",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./apps/**\" --filter \"./services/**\" --filter \"./tools/**\" --filter \"./scripts\" --if-present run typecheck",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:probe-loop": "bats .github/scripts/test/healthz-probe-loop.bats"
  },
  "private": true,
  "devDependencies": {
    "@vitest/coverage-v8": "4.1.5",
    "bats": "1.13.0",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2",
    "vitest": "4.1.5"
  },
  "dependencies": {
    "@playwright/test": "^1.59.1",
    "playwright": "^1.59.1"
  }
}
